#  Maintaining the Harness CLI
Describes the release process for the Harness CLI.

## Update HDH Documentation
If a new release is cut. will need to cut a PR against HDH. HDH uses a template approach e.g pulls from an include
so each MD per distribution can be updated here:
https://github.com/harness/developer-hub/tree/main/docs/platform/shared/cli

