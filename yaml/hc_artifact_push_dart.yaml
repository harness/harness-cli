name: hc artifact push dart
synopsis: Push Dart package
description: |
    Push a Dart .tar.gz package to Harness Artifact Registry (HAR)
usage: hc artifact push dart <registry_name> <dart_tar_gz_path> [flags]
options:
    - name: help
      shorthand: h
      default_value: "false"
      usage: help for dart
    - name: pkg-url
      usage: Base URL for the Packages service
inherited_options:
    - name: account
      usage: Account ID (overrides saved config)
    - name: api-url
      usage: Base URL for the API (overrides saved config)
    - name: format
      default_value: table
      usage: 'Output format: table or json'
    - name: org
      usage: Organisation ID (overrides saved config)
    - name: project
      usage: Project ID (overrides saved config)
    - name: token
      usage: Authentication token (overrides saved config)
    - name: verbose
      shorthand: v
      default_value: "false"
      usage: Enable verbose logging to console
see_also:
    - hc artifact push - Push artifacts to Harness Artifact Registry
