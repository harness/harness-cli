gitops:
  name: gitops-application-demo-5
  projectIdentifier: communityeng
  orgIdentifier: default
  type: application
  application:
    metadata:
      namespace: default
      clusterName: demoasd
      labels:
        harness.io/serviceRef: dee
        harness.io/envRef: de
    spec:
      source:
        repoURL: https://github.com/harness-community/harnesscd-example-apps
        path: guestbook
        targetRevision: master
      destination:
        server: https://kubernetes.default.svc
        namespace: default
  agentIdentifier: AGENT_NAME
  clusterIdentifier: demoasd
  repoIdentifier: harnessexample